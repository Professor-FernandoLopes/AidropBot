const { expect } = require("chai")

describe("Template", function () {
  beforeEach(async () => {
    // Deploy contract and setup the test environment
  })

  describe("Deployment", () => {
    // Test the deployment via checking initial state variables?
  })

  describe("Something...", () => {
    beforeEach(async () => {
      // Call your contract function?
    })

    it("Does something...", async () => {
      // Check your smart contract state for expected changes?
      expect(true).to.equal(true)
    })
  })
})
